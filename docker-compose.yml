version: "3.9"

services:
  collective-memory-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: collective-memory-web
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped

